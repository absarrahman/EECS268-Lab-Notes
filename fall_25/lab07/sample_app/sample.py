def inorder(self, current_node, closure):
    # check whether current node is None
    # if none -> Return
    #
    # Call inorder(current_node.left, closure)
    # execute closure -> closure(current_node.entry)
    # Call inorder(current_node.right, closure)
    pass


def postorder(self, current_node, closure):
    # check whether current node is None
    # if none -> Return
    #
    # Call postorder(current_node.left, closure)
    # Call postorder(current_node.right, closure)
    # execute closure -> closure(current_node.entry)
    pass


def preorder(self, current_node, closure):
    # check whether current node is None
    # if none -> Return
    #
    # execute closure -> closure(current_node.entry)
    # Call preorder(current_node.left, closure)
    # Call preorder(current_node.right, closure)
    pass
